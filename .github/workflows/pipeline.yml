name: my pipeline 

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  justTest:
    environment: dev
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - uses: ./.github/workflows/reusable.yml
        with:
          secrets: inherit