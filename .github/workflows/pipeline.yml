name: my pipeline 

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  justTest:
    environment: dev
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/workflows/reusable.yml
        with:
          secrets: inherit