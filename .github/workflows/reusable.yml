name: Reusable workflow example

on:
  workflow_call:
    secrets:
      TOKEN:
        required: true

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - name: show something
      run: |
        echo ${{secrets.TOKEN}}
